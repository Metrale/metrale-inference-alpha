# 2026-09-25: Qwen3.8-Flash-Next: hybrid GDN + QSA sparse attention,
# multi-hyperconnection streams, PLE n-gram injection, MoE, ViT tower and an MTP layer.
#
# crates/config/src/dispatch.rs reads model_type `qwen4_exp` and
# `qwen3_8_flash_next` with the same parser, which sets `qwen4_exp`
# (parsers/qwen4_exp.rs), so only that name is bound in [[model_types]].






[model]
name = "qwen3.8-flash-next"
hf_id = "Qwen/Qwen3.8-Flash-Next"
architecture = "GDN + QSA sparse attention + mHC + PLE n-gram + MoE"
params = "180B"
active_params = "10B"




layers_total = 48
layers_linear_attention = 36
layers_full_attention = 12
layer_pattern = "3:1 (linear, linear, linear, full)"

moe_experts = 512
moe_topk = 10
moe_intermediate = 640
shared_expert_intermediate = 640

hidden_dim = 2560
head_dim = 256
q_heads = 24
kv_heads = 2

linear_attn_key_heads = 16
linear_attn_key_dim = 128
linear_attn_value_heads = 48
linear_attn_value_dim = 128
linear_conv_kernel = 4

vocab_size = 248320
max_position_embeddings = 262144
rope_theta = 10000000
partial_rotary_factor = 0.25
attn_output_gate = true
mrope_interleaved = true
mrope_section = [11, 11, 10]

# 2026-09-25: Multi-hyperconnection: hc_count residual streams of hidden_dim,
# mixed through a low-rank pair of rank hc_lowrank (nvfp4/hyper_connection.cu).


hc_count = 4
hc_lowrank = 320

# 2026-09-25: QSA indexer, on the full-attention layers only.
indexer_n_heads = 4
indexer_head_dim = 128
indexer_budget = 2048
indexer_compress_ratio = 4

# 2026-09-25: PLE n-gram injection at the layers in ple_layer_ids (one-indexed).
# The per-head embedding slices are concatenated: ngram_heads x ngram_dim = hidden_dim.



ngram_size = 3
ngram_heads = 16
ngram_dim = 160
ngram_split_parts = 128
ple_layer_ids = [2]
ple_conv_kernel_size = 4

mtp_layers = 1
mtp_type = "full_attention"
vision = true

# 2026-09-25: The thinking presets use the sampling values of the checkpoint's
# generation_config.json (temperature 1.0, top_p 0.95, top_k 20).




[sampling.thinking_text]
temperature = 1.0
top_p = 0.95
top_k = 20
# 2026-09-25: min_p and top_n_sigma are pinned to 0 so that chat requests using
# these presets do not get the serve defaults --default-min-p 0.08 and
# --default-top-n-sigma 1.0 (crates/server/src/api/chat/sampling_setup.rs).

min_p = 0.0
top_n_sigma = 0.0

[sampling.thinking_coding]
temperature = 1.0
top_p = 0.95
top_k = 20
# 2026-09-25: No request path selects this preset: api/chat/sampling_setup.rs
# picks tools, thinking_text or non_thinking.


min_p = 0.0
top_n_sigma = 0.0

[sampling.non_thinking]
temperature = 0.7
top_p = 0.8
top_k = 20
presence_penalty = 1.5
# 2026-09-25: min_p and top_n_sigma pinned to 0; see [sampling.thinking_text].



min_p = 0.0
top_n_sigma = 0.0

[sampling.tools]
temperature = 0.7
top_p = 0.8
top_k = 20
presence_penalty = 1.5
# 2026-09-25: min_p and top_n_sigma pinned to 0; see [sampling.thinking_text].



min_p = 0.0
top_n_sigma = 0.0

[[model_types]]
model_type = "qwen4_exp"
hidden_size = 2560

# 2026-09-25: [expected_absent]: kernel lookups this model's dispatch may issue
# that do not resolve for this target, each with a reason. The boot kernel gate
# refuses to serve on any other unresolved lookup unless
# --dangerously-allow-unresolved-kernel-lookups is set (crates/kernels/src/ptx_set.rs).







[expected_absent.w4a16]
w4a16_gemm_t_p3 = """
3-deep weight-pipeline twin of w4a16_gemm_t, shipped ONLY in
qwen3.6-27b/nvfp4/w4a16_gemm.cu (49 globals vs this target's slimmer
shadow). layers::tgemm_kernel probes it first, then falls back to
w4a16_gemm_t, which resolves here. The fallback is the 2-stage parent
the p3 twin was derived from; the twin is UNMEASURED for this target's
shapes — treat as a candidate to measure, not a confirmed loss."""
w4a16_gemm_t_k64_p3 = """
Same family: shipped only in qwen3.6-27b's shadow. layers::k64_kernel
probes it, then REQUIRES w4a16_gemm_t_k64, which resolves here."""
w4a16_gemm_t_k64_n64_p3 = """
Narrow-N deep-K twin, shipped only in qwen3.6-27b's shadow.
layers::k64_n64_kernel returns 0-handle when absent; dispatch gates on
handle != 0 before firing, so the wide-K64 tile serves these shapes."""
w4a16_gemm_t_m128_bf16 = """
BF16-projection prefill twin of w4a16_gemm_t_m128. prefill_weights
gates the probe result (handle != 0); the fallback is the w4a16 m128
twin that resolves here, applied through the standard dequant."""

[expected_absent.w4a4]
w4a4_gemm_mfast = """
This model's w4a4_gemm.cu defines only w4a4_gemm; the mfast variant
exists solely in nemotron-labs-3-puzzle-75b-a9b. Dispatch
(qwen3_attention/init.rs) falls back to w4a4_gemm — same family the
27B declares. UNMEASURED for this target."""

[expected_absent.moe_w4a16]
moe_w4a16_grouped_gemm_ptrtable_e8m0 = """
Native-MXFP4 (E8M0) scale-format variant. Shipped only in
deepseek-v4-flash's moe_w4a16 module (ARM-2 Phase-K). This target is
NVFP4; the ptrtable grouped GEMM that resolves here serves it."""
moe_w4a16_grouped_gemm_ptrtable_t_e8m0 = "Same E8M0 family — NVFP4 target uses the resolved ptrtable_t variant."
moe_w4a16_grouped_gemm_ptrtable_t_k64_e8m0 = "Same E8M0 family — NVFP4 target uses the resolved ptrtable_t_k64 variant."
moe_w4a16_fused_gate_up_t_e8m0 = "Same E8M0 family — NVFP4 target uses the resolved fused_gate_up variant."
moe_w4a16_fused_gate_up_t_k64_e8m0 = "Same E8M0 family — NVFP4 target uses the resolved fused_gate_up_k64 variant."
moe_w4a16_fused_gate_up_t_k64_m128 = """
M=128 Block-D variant shipped only in minimax-m2-229b's shadow (per the
dispatch comment in moe/init.rs). Dispatch falls through to M=64."""

# 2026-09-25: The two f16 entries below are not fallbacks. With
# `--ssm-h-dtype f16` the GDN decode arms launch gdn_f16_norm_k and
# gdn_f16_strided_norm_half_k without checking the handle
# (model-layers layers/qwen3_ssm/ssm_forward.rs and
# trait_decode_multi_seq/ssm_batched_recurrent.rs), so on this target that
# flag launches a zero kernel handle.

[expected_absent.gated_delta_rule]
gated_delta_rule_decode_f16_norm = "FP16 h-state twin; see honesty note in this MODEL.toml. F32 fused-norm arm resolves."
gated_delta_rule_decode_f16_strided_norm_half = "FP16 h-state twin; see honesty note in this MODEL.toml. F32 strided-norm arm resolves."
gated_delta_rule_decode_f32_strided_norm_half = """
Half-register twin of the resolved f32_strided_norm; selected only when
the resolved half-register gate is set, and that gate is off by default
(METRALE_GDN_HALF_REG presence). Not a live path as shipped."""
gated_delta_rule_decode_f32_strided_norm_smem = """
SMEM-staged twin; gated behind METRALE_GDN_SMEM_STAGE (presence, off by
default) AND the half-register gate. Not a live path as shipped."""

# 2026-09-25: The exact-verify twins below ship only in qwen3.6-27b/nvfp4. The
# exact chain runs only with --exact-verify (qwen3_ssm/gdn_flags.rs), and its
# sites check each handle before use (trait_decode_batched_conv_gdn_exact.rs,
# trait_decode_batched_conv_gdn_multi_exact.rs).
[expected_absent.gated_delta_rule_snap]
gated_delta_rule_decode_f32_norm_snap = """
Exact-verify snapshot twin (#435), shipped only in qwen3.6-27b/nvfp4.
trait_decode_batched_conv_gdn_exact probes handle != 0; absent, the
exact arm uses the parent gdn_f32_norm kernel + copy_d2d snapshots
(same bits, more launches)."""
gated_delta_rule_decode_f32_strided_norm_snap = """
Batched exact-verify snapshot twin (#435), shipped only in
qwen3.6-27b/nvfp4. trait_decode_batched_conv_gdn_multi_exact declines
the batched form on handle 0 and the per-sequence exact loop serves
(still exact)."""

[expected_absent.gdn_verify_fused_conv_kn_f32]
gdn_verify_fused_conv_kn_f32 = """
FP32 fused verify conv for the exact-verify chain (#435), shipped only
in qwen3.6-27b/nvfp4. Probed handle != 0; absent, the exact chain keeps
its per-token conv launches (same bits, more launches)."""

[behavior]
# 2026-09-25: QSA selection copies raw NHD rows out of the paged cache, so the
# decode path (qwen3_attention/decode/attention_forward.rs) and the weight
# preflight (crates/model-weights/src/preflight.rs `check_qsa_kv_dtype`) refuse
# any KV dtype but bf16. An explicit --kv-cache-dtype still takes precedence
# over this default, and without either the engine default is fp8
# (crates/server/src/cli/serve_args.rs `DEFAULT_KV_CACHE_DTYPE`).


default_kv_dtype = "bf16"
# 2026-09-25: The checkpoint's chat template asks for Qwen3-Coder XML tool calls:
#   <tool_call><function=fn><parameter=key>value</parameter></function></tool_call>


tool_call_parser = "qwen3_coder"
# 2026-09-25: Unless enable_thinking is false, the checkpoint's chat template adds
# a reasoning-effort system preamble and opens the assistant turn with `<think>\n`.
# thinking_default = true turns thinking on when no thinking directive reaches the
# request; --disable-thinking overrides it (crates/server/src/api/chat/thinking.rs).






thinking_default = true
thinking_in_tools = true
# 2026-09-25: Chat requests take temperature, top_k, top_p, min_p and top_n_sigma
# from the [sampling.*] presets instead of the serve defaults, unless the request
# sets them (crates/server/src/api/chat/sampling_setup.rs). The key must sit
# under [behavior], the only table build_parse_behavior.rs reads it from.


use_sampling_presets_for_core = true
# 2026-09-25: 0 turns off the minimum-reasoning floor, which otherwise biases
# `</think>` by -8 for the first 16 thinking tokens
# (crates/server/src/scheduler/sample_step/penalties.rs, crates/kernels/build_parse_behavior.rs).





min_reasoning_floor_tokens = 0
# 2026-09-25: When thinking is the only reason an EOS is held back, the EOS
# closes the thinking block (the EOS itself is still dropped) instead of
# thinking going on (crates/server/src/scheduler/decode_logits_step.rs).

honor_eos_inside_thinking = true
max_thinking_budget = 2048
