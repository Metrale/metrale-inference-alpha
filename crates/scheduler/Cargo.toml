# SPDX-License-Identifier: MIT OR Apache-2.0
[package]
name = "metrale-scheduler"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Metrale Engine: the scheduler's I/O contract — step plans, effects, the device/clock/spill router traits, the effect-trace decorator and the one driver loop. No model dependency."

[dependencies]
anyhow = { workspace = true }

[lints]
workspace = true
