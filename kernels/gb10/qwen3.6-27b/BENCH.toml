# 2026-09-25: Benchmarks for this model and the thresholds its gate records must meet, read by
# `metrale_bench::gate::bench` (crates/bench/src/gate/bench.rs). Hardware and model come from this file's path.
#
# Thresholds are per checkpoint: each [[benchmarks]] entry names one.
#
# `taxon::configs` does not list this file, so it is outside the closure hash: editing a threshold does not change
# any target's closure hash.
#
# An entry with `status = "unmeasured"` may not carry a [benchmarks.metrics] table (loading refuses one), and
# baseline_for skips it.







[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.6-27B-NVFP4"
gate = "bfcl-subset"
recipe = "qwen3.6/qwen3.6-27b-nvfp4-unsloth"





# 2026-09-25: Not the bfcl-subset default: kernels/gb10/qwen3.8-27b/BENCH.toml holds the default entry. A gate run
# reaches this entry with `--pull-request-gate --checkpoint unsloth/Qwen3.6-27B-NVFP4`.
default = false
status = "measured"
note = """\
Gate D. Golden ST-995 draw (62/10/10, floor 25, n=995, seed 42, temp 0) on the DENSE 27B — \
the MLPerf-edge submission model, which is the model these numbers are defined for. \
THRESHOLDS ARE THE HIGH-WATER RATCHET, NOT THE FLOOR: 87.44 overall is the best \
like-for-like value ever recorded under these serve flags (2026-07-13 prestack b45af988); \
88.59 normalized is the best (2026-07-26 pr369 de6153f3). Ratcheting to best-ever rather \
than to a survivable floor is deliberate — it catches regression from our best instead of \
only catching catastrophe, and it stops repeated sub-noise dips silently walking the gate \
down over successive PRs. `noise` is the MEASURED +/-0.4 MTP-greedy-nondeterminism band on \
this model: a bit-identical change has swung overall by 0.40, so a dip inside it is not \
signal. The MLPerf-edge floor (83.64 / 85.32, = llama.cpp Q4_K_M golden 86.23/87.96 x 0.97) \
is NOT repeated as a field here because this schema enforces only `metrics` and an extra key \
would be silently ignored — it is subsumed, since 87.44 > 83.64 and 88.59 > 85.32. If these \
mins are ever lowered, re-check them against that floor by hand: dropping below it is an \
outright MLPerf submission failure, not a regression. Do NOT score the echolp n=1004 draw \
against these numbers — see bfcl-subset-echolp. History: 2026-07-13 prestack b45af988 \
87.44/88.53 (overall high-water); 2026-07-13 main d77bdf84 87.24/88.40; 2026-07-26 pr369 \
de6153f3 87.34/88.59 (normalized high-water); 2026-08-03 PR #388 Gate D 87.74/89.43 n=995. \
The draw is PINNED at n=995: a score from a different draw is not comparable to these \
thresholds, and the sample count is the only thing that reveals it after the fact, so it is \
gated exactly rather than merely recorded."""

[benchmarks.metrics.normalized_single_turn_score]
min = 88.59
noise = 0.4

[benchmarks.metrics.overall_accuracy]
min = 87.44
noise = 0.4

[benchmarks.metrics.samples]
min = 995
max = 995

[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.6-27B-NVFP4"
gate = "concurrency-sweep"
recipe = "qwen3.6/qwen3.6-27b-nvfp4-unsloth"












# 2026-09-25: default = false because qwen3.8-27b holds the measured concurrency-sweep default on gb10, and
# baseline_for refuses two measured defaults on one hardware, which would fail the gate for every checkpoint. A
# default = false entry cannot collide whatever its status becomes.
default = false
status = "unmeasured"
note = """\
The DENSE half of the concurrency curve. Declared here so the intent is on \
record for both architectures: the C=1..32 latency/throughput ladder is \
measured on dense AND MoE, because they do not share a decode path — the \
dense FFN batches over all rows via forward_prefill while a 256-expert MoE \
takes the grouped-GEMM arm, and a regression can hit one without touching \
the other. A curve measured on one model cannot speak for the other.

NO [benchmarks.metrics] TABLE ON PURPOSE. concurrency-sweep is a promotion \
candidate HISTORICALLY; since 2026-08-15 concurrency-sweep IS REQUIRED, with \
its measured subject on qwen3.8-27b — this 3.6 entry stays an unmeasured \
declaration. It has never been run on main at C=32. \
A guessed threshold a run could clear would report PASS for something nobody \
measured — the exact failure this file's header warns about. Thresholds land \
here after the first measured run, and that run is what promotes the bench \
from PROMOTION_CANDIDATES to REQUIRED.\
"""

[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.6-27B-NVFP4"
gate = "agentic-webserver"
recipe = "qwen3.6/qwen3.6-27b-nvfp4-unsloth"
default = false
status = "unmeasured"
note = """\
The DENSE agentic leg — REGISTERED 2026-08-14 SO THE 27B CAN BE BASELINED, NEVER YET \
MEASURED. No agentic-webserver number has ever been recorded on this checkpoint, and it \
cannot inherit the 35B's ceiling: the 35B is A3B (~3B active parameters per token) while \
the dense 27B activates all 27B on every token, so on a memory-bound GB10 its per-token \
decode is several times heavier and its agentic Σwall will land substantially higher — \
expected above the 35B's entire ceiling (>1200 s per the owner's estimate; the 35B's bound \
in ITS entry is 1800 s: it was 1000, and was raised on 2026-08-18 once five \
10/10 tiers measured 774-1039 s and showed 1000 sat inside the healthy band. \
Speed there is bounded by a measured `s_per_turn` metric instead; this entry \
has no such measurement, so it is NOT speed-gated. Per this file's header, \
thresholds are per CHECKPOINT — the 35B's bounds simply do not apply here. || NO \
[benchmarks.metrics] TABLE ON PURPOSE, and in particular NO Σwall floor or ceiling: this \
repo's recurring failure mode is a plausible-looking number written down before anyone \
measured it (the 384 inter-tool prose budget, the hardcoded 256 effort rung, the P2-1 \
default), and a guessed ceiling either passes vacuously or fails honest PRs. `default = \
false` — the 35B owns this gate's default serve — and `baseline_for` drops unmeasured \
entries anyway, so `--pull-request-gate` cannot serve this leg and a record against this \
checkpoint fails `check_record` loudly with "no baseline for model" rather than reading as \
PASS. Until floors land here, every dense run is informational. || ★ WHOEVER WRITES THE \
DENSE CEILING MUST FIRST DECIDE WHAT IT IS. The 35B's Σwall is documented in its entry as \
a BLOWUP DETECTOR with generous headroom, NOT a perf bound — it cannot see a 5-10% TTFT \
regression, which is what the two TTFT gates are for. A tight ceiling here silently \
converts the blowup detector into a perf gate that every unrelated PR then pays for. \
Whichever is chosen, state it in this note beside the number. || CALIBRATION PROTOCOL, in \
order, before any floors: (1) serve this entry's recipe on an IDLE box — walls are \
box-specific and Σwall carries ±200-300 s of outlier noise on the 35B; a co-tenant makes \
the number meaningless; (2) drive it manually, since the gate cannot serve an unmeasured \
entry: `met benchmark run agentic-webserver --yes --url <serve> --model \
unsloth/Qwen3.6-27B-NVFP4 --param wall_budget_s=100000` — the wall_budget_s override \
neutralizes the leg's 35B-tuned 1000 s default so the run verdict does not encode another \
checkpoint's ceiling; iterations stays at the pinned 10; (3) ≥3 repeats, so the spread is \
MEASURED rather than assumed — the dense band is unknown; (4) ratchet from the measurement \
with the run's sha, date and box recorded beside the numbers, the way bfcl-subset-echolp's \
floors are; (5) only then flip status = "measured". || RE-POINTING AT QWEN3.8-27B (owned \
by a separate workstream) is a checkpoint/recipe swap, not a rewrite: change `checkpoint` \
and `recipe` here (or add the sibling entry under kernels/gb10/qwen3.8-27b once that \
target exists) and add its family to the agentic descriptor's `intended_for` list in \
crates/metrale-plugin/src/benchmarks/agentic/descriptors.rs. Thresholds are per checkpoint: \
nothing measured on Qwen3.6 transfers, and the calibration protocol runs again from (1).\
"""




















































































# 2026-09-25: vision-fidelity metrics (crates/bench/src/benchmarks/vision/driver.rs): geometry_matched counts the
# geometry cells that matched, geometry_asserted the cells measured at all (matched or not), probes_passed the
# capability probes that passed, control_held whether the no-image control held, and integrity_passed /
# integrity_measured the integrity legs that passed and that were measured. The bounds carry no noise term.

[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.6-27B-NVFP4"
gate = "vision-fidelity"
recipe = "qwen3.6/qwen3.6-27b-nvfp4-unsloth"
label = "Qwen3.6-27B dense (NVFP4, unsloth)"
default = false
status = "measured"
note = """\
REFERENCE RUN 2026-08-14, dgx-00, binary feat/video-support @ 5a22d7ce: PASS, 14/14 geometry \
cells matched, 3/3 probes, 10/10 integrity legs, control held, 29.3 s. Geometry was IDENTICAL across all \
three vision targets (49/121/192/220/576/576/920/405/1400/1/128/49/49/49 merged \
tokens) -- token \
count is a property of the preprocessor rather than the checkpoint, so agreement \
across a dense NVFP4, an unsloth NVFP4 and an FP8 MoE is evidence the arithmetic is \
right rather than a coincidence. * THIS is the checkpoint that exposed the resolver \
defect. It ships processor_config.json and NO preprocessor_config.json, and Metrale Engine read \
only the latter -- so it declared 16777216 px under image_processor.size and was \
served at the 1280px fallback with a 6400-patch encoder anyway. An earlier record here \
said this checkpoint declared no bound; that was the missing reader, not the \
checkpoint. With the fix it resolves 16.7M px, capacity clamps to 16384, and rung 09 \
reports 1400 where the fallback produced 920 -- the gate catching exactly the class of \
defect it was built for, on a real model rather than in a unit test."""

[benchmarks.metrics.geometry_matched]
min = 14.0

[benchmarks.metrics.geometry_asserted]
min = 14.0

[benchmarks.metrics.integrity_passed]
min = 10.0

[benchmarks.metrics.integrity_measured]
min = 10.0

[benchmarks.metrics.probes_passed]
min = 3.0

[benchmarks.metrics.control_held]
min = 1.0
























# 2026-09-25: video-fidelity. On a serve without --video-allow-ffmpeg the server cannot decode the non-GIF clips,
# and those legs report Skipped (crates/bench/src/benchmarks/video/driver.rs, skip_if_decoder_unavailable). This
# entry's legs_skipped max = 0 then fails the gate, so a passing record has exercised the ffmpeg path.

[[benchmarks]]
quant = "nvfp4"
checkpoint = "unsloth/Qwen3.6-27B-NVFP4"
gate = "video-fidelity"
recipe = "qwen3.6/qwen3.6-27b-nvfp4-unsloth"
label = "Qwen3.6-27B dense (NVFP4, unsloth)"





# 2026-09-25: The video-fidelity default on gb10; the qwen3.8-27b and qwen3.6-35b-a3b entries are default = false.
default = true
status = "measured"
note = """\
REFERENCE RUN 2026-08-14, dgx-00, binary feat/video-support: PASS, 13/13 legs, 0 \
skipped, control held. Confirms video is not specific to the Qwen3.8 checkpoint — all \
three vision targets declare <|video_pad|> 248057 and share the Qwen3-VL temporal \
geometry, and this one decodes, groups and orders a clip identically. ★ THIS NOTE USED \
TO END "`default = false`: qwen3.8-27b owns this gate's default serve", which stopped \
being true on 2026-08-15: this target IS the gate's required subject and the key above \
reads `default = true`. The reason for the flip is in the comment directly above it."""

[benchmarks.metrics.legs_passed]
min = 13.0

[benchmarks.metrics.legs_skipped]
max = 0.0

[benchmarks.metrics.control_held]
min = 1.0
