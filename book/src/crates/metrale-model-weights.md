# metrale-model-weights

**Path:** `crates/model-weights/`

The weight store and loaders: safetensors, the fast weight loader (`src/fast_weights/`), the RDMA weight and LoRA tiers, and preflight (`src/preflight.rs`).
