# 2026-09-25: Kernel target for stepfun-ai/Step-3.7-Flash-NVFP4 on GB10. nvfp4/KERNEL.toml compiles
# gb10/common with -DHDIM=128 (as minimax-m2-229b does), its own moe_silu_mul.cu, and the minimax-m2-229b
# and deepseek-v4-flash sources it uses. moe_topk_sigmoid (gb10/common/moe_topk_sigmoid.cu) reads at most
# MAX_EXPERTS = 512 router logits; moe_experts below is 288.















[model]
name = "step3p7-flash"
hf_id = "stepfun-ai/Step-3.7-Flash-NVFP4"
params = "196B"
active_params = "~11B"
architecture = "Full Attention + Sliding Attention + 288-expert Sigmoid-Routed MoE + Shared Expert + MTP K=3"

layers_total = 45
layers_linear_attention = 0
layers_full_attention = 12
layers_sliding_attention = 33
layer_pattern = "mixed attention (12 full + 33 sliding in 45 hidden layers, 1:3 full:sliding ratio + 3 sliding MTP = 48 total)"

moe_experts = 288
moe_topk = 8
moe_intermediate = 1280
shared_expert_intermediate = 1280
moe_scoring_func = "sigmoid"
moe_routing_bias = true

hidden_dim = 4096
head_dim = 128
q_heads = 64
kv_heads = 8


rotary_dim = 64
rope_theta = 500000
partial_rotary_factor = 0.5


qk_norm_type = "per_head"

vocab_size = 128896
max_position_embeddings = 262144
attn_output_gate = true
sliding_window = 512


mtp_layers = 3
mtp_transformer_layers = 1
mtp_type = "full_attention"

vision = true

[sampling.default]
temperature = 0.7
top_p = 0.95
top_k = 20

[behavior]
thinking_default = true
thinking_in_tools = false
max_thinking_budget = 2048







default_kv_dtype = "bf16"
fp8_kv_calibration_tokens = 256

[[model_types]]
model_type = "step3p7"
hidden_size = 4096

[kernel]

moe_gate_shmem_bytes = 8192

# 2026-09-25: Lookups this target's dispatch may issue and fail to resolve without failing the boot
# audit (crates/kernels/build_parse.rs parse_expected_absent); each value is the stated reason.







[expected_absent.w4a16]
w4a16_gemm_t_p3 = """
3-deep weight-pipeline twin shipped ONLY in qwen3.6-27b's shadow.
layers::tgemm_kernel probes it, then falls back to w4a16_gemm_t, which
resolves on this target's shadow. UNMEASURED — candidate, not loss."""
w4a16_gemm_t_k64_p3 = "Same family; layers::k64_kernel falls back to the required w4a16_gemm_t_k64 (resolves on this target's shadow)."
w4a16_gemm_t_k64_n64_p3 = "Narrow-N deep-K twin; layers::k64_n64_kernel returns 0-handle when absent, dispatch gates on handle != 0 before firing."
w4a16_gemm_t_m128_bf16 = "BF16-projection prefill twin of w4a16_gemm_t_m128; prefill_weights gates on handle != 0, falls back through the resolved w4a16 m128 path."
w4a16_gemm_t_m128_bf16_v2 = "Second-generation BF16-projection twin (w4a16_v2_kernel); same handle != 0 gate, falls back to the resolved m128 arm."
int8_gemm_faith2 = """
Lossy W4A8 dense-FFN prefill twin shipped in qwen3.6-27b-class shadows.
Dispatch is DOUBLE-GATED: handle != 0 AND the METRALE_INT8_PREFILL env opt-in
(dense_ffn.rs:1512), so the resolved NVFP4 prefill path serves by default.
UNMEASURED."""
int8_gemm_i32acc = "i32-accumulator variant of the same opt-in int8 family (METRALE_INT8_PREFILL); handle != 0 gate in dense_ffn.rs."
requant_w_nvfp4_int8 = "Weight requant helper for the opt-in int8 family — only materialized when METRALE_INT8_PREFILL is set and the int8 GEMM handles resolve."
requant_a_bf16_int8 = "Activation requant helper for the same opt-in int8 family; identical gating."

[expected_absent.w4a4]
w4a4_gemm = """
Dense-FFN native-FP4 prefill arm (METRALE_FP4_PREFILL env opt-in, lossy
cos~0.99). Dispatch gates on handle != 0 AND the env flag
(dense_ffn.rs:1585); shipped in qwen3.6-27b-class shadows only.
UNMEASURED."""
w4a4_gemm_mfast = """
Shipped only in nemotron-labs-3-puzzle-75b-a9b's shadow. The attention
W4A4 prefill path is additionally OPT-IN ONLY (METRALE_ATTN_W4A4 env) and
dispatch gates on handle != 0 (paged_qkv.rs:49, paged_oproj.rs:32).
UNMEASURED."""

[expected_absent.moe_w4a16]
moe_w4a16_grouped_gemm_ptrtable_e8m0 = """
Native-MXFP4 (E8M0) scale-format variant shipped only in
deepseek-v4-flash's shadow. This NVFP4 checkpoint's scales are the
modelopt NVFP4 format, so dispatch uses the resolved ptrtable grouped
GEMM."""
moe_w4a16_grouped_gemm_ptrtable_t_e8m0 = "Same E8M0 family — NVFP4 target uses the resolved ptrtable_t variant."
moe_w4a16_grouped_gemm_ptrtable_t_k64_e8m0 = "Same E8M0 family — NVFP4 target uses the resolved ptrtable_t_k64 variant."
moe_w4a16_fused_gate_up_t_e8m0 = "Same E8M0 family — NVFP4 target uses the resolved fused_gate_up variant."
moe_w4a16_fused_gate_up_t_k64_e8m0 = "Same E8M0 family — NVFP4 target uses the resolved fused_gate_up_k64 variant."
moe_w4a16_fused_gate_up_t_k64_fp4 = "FUSED-FP4 gate_up (Holo-3.1 family); forward_prefill_routed gates on gateup_fp4 && handle != 0 before firing."
moe_w4a16_down_t_k64_fp4 = "FP4 down-projection twin; forward_prefill_routed gates on down_fp4 && handle != 0."

[expected_absent.nvfp4_mmq]
metrale_nvfp4_mmq128_nc = """
Vendored llama Blackwell block-scale FP4 MMQ dense-FFN prefill family
(shipped in qwen3.6-27b's shadow). finalize gates on ALL family handles
!= 0 and only fires for METRALE_FFN_NVFP4_MMQ opt-in SiLU models
(dense_ffn.rs:473); the resolved t_m128 prefill path serves by default.
UNMEASURED."""
metrale_nvfp4_mmq128_wc = "Same MMQ family — family-wide handle gate plus METRALE_FFN_NVFP4_MMQ opt-in."
metrale_nvfp4_mmq16_nc = "Same MMQ family — family-wide handle gate plus METRALE_FFN_NVFP4_MMQ opt-in."
metrale_nvfp4_mmq16_wc = "Same MMQ family — family-wide handle gate plus METRALE_FFN_NVFP4_MMQ opt-in."
metrale_nvfp4_mmq32_nc = "Same MMQ family — family-wide handle gate plus METRALE_FFN_NVFP4_MMQ opt-in."
metrale_nvfp4_mmq32_wc = "Same MMQ family — family-wide handle gate plus METRALE_FFN_NVFP4_MMQ opt-in."
metrale_nvfp4_mmq64_nc = "Same MMQ family — family-wide handle gate plus METRALE_FFN_NVFP4_MMQ opt-in."
metrale_nvfp4_mmq64_wc = "Same MMQ family — family-wide handle gate plus METRALE_FFN_NVFP4_MMQ opt-in."
metrale_nvfp4_quantize_bf16 = "MMQ activation-quantize helper — only materialized when the MMQ family resolves and is opted in."
metrale_nvfp4_repack = "MMQ weight-repack helper — same family-wide gating."
metrale_nvfp4_scale_bf16 = "MMQ scale helper — same family-wide gating."
metrale_nvfp4_silu_mul_quant = "MMQ fused SiLU-mul-quant helper — same family-wide gating."
metrale_nvfp4_silu_mul_scaled = "MMQ fused scaled-SiLU-mul helper — same family-wide gating."

[expected_absent.q4k_mmq]
metrale_q4k_mmq128_nc = """
Q4_K FFN MMQ family (METRALE_FFN_MMQ opt-in). finalize_q4k_load gates on
ALL four q4k handles != 0 AND the env flag (dense_ffn.rs:382); shipped
in qwen3.6-27b-class shadows only. The resolved NVFP4 path serves by
default. UNMEASURED."""
metrale_q4k_mmq128_wc = "Same Q4_K family — family-wide handle gate plus METRALE_FFN_MMQ opt-in."
metrale_q8_1_quantize_ds4_bf16 = "Q4_K activation-quantize helper — only materialized under the same family-wide gating."

[expected_absent.q4k_quantize]
q4k_quantize = "Q4_K weight-quantize helper — same family-wide gating as the q4k_mmq arms above."
