blank_issues_enabled: false
contact_links:
  - name: Security disclosure
    # Must match the canonical repository (Cargo.toml `repository`). The owner
    # is the org `Metrale`, not `metrale` — `metrale` is an
    # unrelated personal account and `metrale/metrale` does not exist, so the old
    # link 404'd and a reporter who followed it had nowhere to file.
    url: https://github.com/Metrale/metrale-inference-alpha/security/advisories/new
    about: Report a vulnerability privately via GitHub Security Advisories. See SECURITY.md.
