# metrale-model-layers

**Path:** `crates/model-layers/`

Generic layers (attention, SSM, MoE, FFN, norm, MTP heads, vision, ops), LoRA, the weight map and the draft-proposer contract. `TransformerLayer` is in `src/layer/`, the layer implementations in `src/layers/`, and the quant-format dispatch in `src/quant_format/`.
